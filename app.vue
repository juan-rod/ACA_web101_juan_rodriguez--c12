<template>
  <div class="nav">
    nav
  </div>
</template>

<script>
export default {
  name: 'NavBar',
  methods: {
    triggerBounce(element) {
      element.classList.toggle('')
    }
    t () {

      if (name === "juan") {
        return yes
      } else {
        return 'no'
      }

      return name === 'juan'
        ? 'yes'
        : 'no'

      return user.id
    }
  }
}
</script>

<style lang="scss" scoped>

</style>